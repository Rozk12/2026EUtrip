# Basic Auth (both required to enable). If unset, the site is public.
BASIC_AUTH_USER=
BASIC_AUTH_PASS=

# Google Sheets (optional). Both required to enable the Sheet data source.
GOOGLE_SHEET_ID=
GOOGLE_SERVICE_ACCOUNT_JSON=
